# v2.6.1 안정화 트리아지 종합 — Sol 지시서 결함 RED 재현 (2026-07-23)

> 기준: main HEAD `8ddebc7` (v2.6.0). 지시서=`docs/reviews/2026-07-23-sol-v261-stabilization-handoff.md`.
> 방법: 3-AI 병렬 재현(codex@영진 P0 / agy P1·문서 / ultracode 경합). 규칙=수정 전 최신 main에서 실패 재현(지시서 §16 규칙1).
> 원본: `tmp/v261-triage-{codex,agy,ultracode}.md` + `tmp/v261-triage/**/result.json`.

## 종합 판정: 지시서 P0/P1/문서 결함 **전건 재현 확정** (13/13)

| ID | 우선순위 | 판정 | 근본원인 (파일:라인) | 담당 재현 |
|---|---|---|---|---|
| R2-05 shell command-position | P0 | **재현 O** (7/7 미차단) | `core/destructive_guard.py:23,395-422`(경계 4종만)·`:239-240`(tokens[0]만)·`:35-56`(wrapper에 command/exec 없음) | codex |
| R2-05B git clustered force | P0 | **재현 O** (3/3 allow) | `core/destructive_guard.py:27,232`(exact-token만)·`:234`(브랜치예외 exact)·`:236`(cluster를 pathspec 오인) | codex |
| STATE-02 publish-boundary write loss | P0 | **재현 O** (10/10 split) | `goals/goals.py:73-78`가 layout barrier 없이 write·`core/state_migration.py:402-407`(검사~rename window) | codex |
| STALE-01 mutation StaleTurn | P0/P1 | **재현 O** | deny 조건이 `identity_conflict`까지 AND 요구(수정조건 검증 시 deny됨) | ultracode |
| QUAR-01 quarantine overwrite | P1 | **재현 O** (16호출→3잔존, 13유실) | `core/quarantine.py:43-50`(exists check)·`:115`(os.replace overwrite) | agy |
| GOALS-02 identity UX | P1 | **재현 O** | `goals/goals.py:38-41,183-190`(identity None→legacy)·`core/gate_counters.py:211-224`(multi-identity가 legacy 무시) | agy |
| CONFIG-02 TOML declaration | P1 | **재현 O** (ABSENT 반환) | `adapters/claude_code/project_config.py:20-22`(정규식이 quoted/dotted 미매칭)·`:126-129`(malformed→ABSENT)·`:75-81`(legacy fallback) | agy |
| MIGRATION-02 archive rotation | P1/P2 | **재현 O** | `core/ledger_migration.py:271-272`(full-byte 비교)·`:24,206-207`(단일 고정 archive 미회전) | ultracode |
| ATTR-02 symlink attribution | P1/P2 | **재현 O** | candidate가 resolved(real.txt)만 저장, symlink replace delta=logical(link.txt) 불일치 | ultracode |
| DOC-02 README stale | P1 | **확정** (5종 100% 현재) | `README.md:94,99,101,123,138,183`·`README.ko.md:86,89,101,118,161` | agy |

## 지시서 팩트체크 (지시서도 검증 대상 — 오류 걸러냄)

- **REL-02 "공개 최신 릴리스가 2.1.0에 머묾" → 기각**: 좌상 실측 GitHub latest=`v2.6.0`(어제 발행분 정상). v2.6.1 릴리스 절차만 남음, "2.1.0 머묾" 전제는 오류.
- 나머지 결함 주장은 전부 코드 근거·재현 수치로 확정. 지시서 신뢰도 높음.

## PR 착수 계획 (지시서 §14 분할안 채택)

- **PR A** = R2-05 + R2-05B + R2-06(tee benign) — command-position tokenizer/state machine 재구현. **RED 회귀 테스트 선행**(재현 corpus를 그대로 실패 테스트로).
- **PR B** = STATE-02(가장 위험, 독립) — 공통 layout write barrier.
- **PR C** = STALE-01(3 adapter 동일 invariant).
- **PR D** = QUAR-01 + QUAR-02(원자 예약 O_EXCL + truncation metadata).
- **PR E** = GOALS-02 + CLI goals.
- **PR F** = CONFIG-02 + MIGRATION-02.
- **PR G** = ATTR-02 + HINT-02.
- **PR H** = CLI-02(doctor/init/status) + COMPAT-01/02.
- **PR I** = DOC-02(README 전면) — PR H 뒤(smtw init/doctor 존재 후).
- **PR J** = CI-02 + REL-02 + GOV-01 + v2.6.1 릴리스.

각 PR: 최신 main에서 RED 재현 → 수정 → focused+full pytest+ruff+compileall+probes+e2e+wheel → P4 교차검토 → CI green 머지. INV-01~10 불변식 준수(특히 R2 fail-closed 무약화). P0 전건 green 전 신규 gate/host 착수 금지.
