# v2.6.1 PR A (R2 셸 파서) P4 교차검토 종합 — 2026-07-23

> 대상: `worker/v261-pr-a-r2-parser` (bf9417c → 725429b → 485bb1a)
> 원본: `tmp/v261-pr-a-{agy,ultracode}.md`·`tmp/v261-pr-a-reverify{,3}.md`·gate 로그 + baseline 대조(좌상)

## 최종 판정: **APPROVE** (왕복 2회 수렴, 신규 우회 0)

### 검토 경과
- **1차(bf9417c)**: 좌상 게이트 pytest 1164 green / agy 적대 리뷰 APPROVE 결함 0 / **ultracode 실측이 우회 5건+오탐 2건 적발**(agy가 백틱 미검사로 놓친 것 — 리뷰만으론 부족, 실주입이 잡음).
- **1차 보완(725429b)**: 핵심 7건 RED→green, clean venv 1190. 그러나 ultracode 완전 재실측이 **신규 우회 25건** 적발 → 종합 FAIL. ⚠️좌상이 result.json 부분요약(fixes8 PASS)만 보고 성급 APPROVE·PR#19 생성 → **완전 verdict(FAIL) 확인 후 draft로 회수**(교훈: 워커 결과는 최종 verdict·adversarial 차원까지 끝까지 확인).
- **2차 보완(485bb1a)**: SEVERE 2패밀리(미등록 셸 dash/zsh, if/while 조건) + 저비용 7종 차단. RED 35→green, clean venv 1249.
- **3차 재실측(ultracode)**: **families 33/33 CLOSED·corpus PASS(19+19)·신규 우회 0·오탐 0**. 지난 25건 전건 종결.

### 종결한 결함
- 지시서 §5/§13(R2-05/05B/06) + 1차 P4 7건(백틱·case·here-string·busybox·find-exec·heredoc오탐·ANSI-C오탐)
- 2차 25건(A 미등록셸 dash/zsh/ash/ksh/csh·B if/while/until 조건·C find -ok/-delete·D coproc·E 대괄호글롭 r[m]·F 프로세스치환 <()·G 다중행case+`|`·H 밀착here-string·I env --split·J 함수정의·이중백틱중첩)

### Known Limitation (INV-10 ACCEPTED_RISK, CHANGELOG·PR 정직 기록)
- `eval `echo rm x`` — echo 런타임 출력을 eval 재해석하는 2단계 동적. 정적 파싱 본질 불가.
- `grep "` + "`" + `" file.txt` — 큰따옴표 안 외따로 백틱. bash 자신도 unexpected EOF로 거부하는 malformed → fail-closed 정당(INV-01).
- 무한 창의적 난독화는 v-next 후보(HINT-02 계열). R2 완전성은 비보장(지시서 §8.2).

### 교훈
- 리뷰(코드 읽기) ≠ 실측(주입). R2 같은 보안 파서는 agy APPROVE라도 ultracode 실주입 필수 — 실제로 우회 5+25건을 실측만 잡음.
- 워커 result.json은 부분 요약이 아니라 최종 verdict까지 확인 후 판정.
- R2 파서 완전성은 무한 — 왕복 2~3회에서 발견 감소 시 범위 확정 + Known Limitation 수용(무한 반복 금지).
