# v2.6.1 PR B (STATE-02 write barrier) P4 교차검토 종합 — 2026-07-23

> 대상: `worker/v261-pr-b-state-barrier` `26d4a5d` (core/state_layout.py +52·quarantine·state_migration·goals + test +273)
> 원본: `tmp/v261-pr-b-agy.md` + `tmp/v261-pr-b-gate.txt` + Codex 구현 실측

## 판정: **APPROVE — critical/high 0, INV-03/04 실증**

STATE-02는 동시성 race 성격(R2 파서의 corpus 우회와 달라) → Codex multi-process 실측 + agy 리뷰 + 좌상 게이트로 충분.

### 검토별 결과
- **Codex 구현 실측**: 8 writer × 100 writes(800회) 동시 race에서 **성공반환 write 유실 0, authority split 0**, crash-cut 5종 복구, migration 44/44·writers 131/131·R2 140/140 회귀 green. (POSIX는 호스트 부재로 CI 확인 대상.)
- **agy 적대 리뷰**: APPROVE, 결함 0. INV-03(state_write_scope에서 owner_lock 후 authority 재판정)·INV-04(800회 유실 0)·lock 순서(5 writer 적용)·성능(quarantine wait_seconds=0으로 deny 비지연)·crash-cut 5종 전항 PASS.
- **좌상 게이트 이종 재실행**: pytest **1087 passed·1 skipped**(24m), ruff green.

### 핵심 설계
- `state_write_scope(root)`: layout lock 획득 → authority 재판정 → write. writer가 lock 전 계산한 path 재사용 금지(INV-03).
- 일반 hook·quarantine backup은 `wait_seconds=0` fail-open 분리 → R2 deny decision이 lock으로 지연되지 않음(성능 INV).
- 적용: ledger·goals·intent·agent_log·provenance_store·quarantine 등.

### 잔여
- POSIX 멀티프로세스는 호스트에 WSL/container 부재로 미실행 → CI(ubuntu) 확인 필수.
- **최종: PR B APPROVE**, CI green 확인 후 머지.
