# v2.6.1 PR C (STALE-01 mutation deny) P4 교차검토 종합 — 2026-07-23

> 대상: `worker/v261-pr-c-stale-turn` `300c6d7` (adapters/{claude_code,codex_cli}/pre_tool_use.py·antigravity/oma_hook.py 각 +11 + test +290)
> 원본: `tmp/v261-pr-c-agy.md` + 좌상 diff 검토 + Codex 구현 실측

## 판정: **APPROVE — critical/high 0, INV-05 충족**

STALE-01은 로직 조건 변경(단순·명확)이라 R2 파서 같은 대규모 적대 실측 불필요 → 좌상 diff 검토 + agy 리뷰 + 게이트로 검증.

### 핵심 변경 (지시서 STALE-01 정확 반영)
- **이전**: `StaleTurn AND identity_conflict AND mutation_capable` → deny (identity_conflict가 이전 resolve 기준 false면 race 통과)
- **이후**: `StaleTurn AND mutation_capable` → deny. `identity_conflict`는 message detail로만(`detail = " identity conflict" if invocation.identity_conflict else ""`).
- 3 어댑터(Claude Code·Codex CLI·Antigravity) 대칭 적용(INV-05).

### 검토별 결과
- **좌상 diff 검토**: claude_code/pre_tool_use.py:218 조건이 지시서대로 정확. 3 어댑터 +11 대칭.
- **agy 적대 리뷰**: APPROVE, 결함 0. INV-05·identity_conflict 역할 전환·read-only allow 유지(`test_read_only_stale_turn_keeps_existing_allow_policy` 3어댑터 실증)·3어댑터 대칭·fault injection(ledger invocation 0등록) 전항 PASS.
- **Codex 구현 실측**: RED 선행 → stale_turn_adapters 6 passed, clean venv 1093 passed·ruff·compileall.

### 잔여
- 최종 CI(ubuntu+windows)로 이종 확인 후 머지. → PR C APPROVE.
