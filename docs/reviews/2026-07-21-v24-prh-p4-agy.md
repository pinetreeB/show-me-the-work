# v2.4 PR H 문서/위생 적대 관점 (P4) 검토 보고서

> **작성자**: agy (리뷰·분석 워커)
> **대상**: `worker/v24-pr-h` 브랜치 (커밋 origin/main..worker/v24-pr-h)
> **판정**: **APPROVE** (문서 정확성 및 패키징 진입점 동작 확인)

---

## 1. DOC-01: README 정합성 검토

- **opt-in 필수 표기**: "당신은 아무것도 할 필요 없이, 한 번 설치하면..."이라는 과장된 문구가 "플러그인을 한 번 설치한 뒤, 감독할 프로젝트에서만 config를 켜면..."으로 정확하게 정정되었습니다.
- **Fail-open (캡) 명시**: 무조건 막는다는 절대적 표현이 "최대 두 번 기계적으로 되돌려 보내며, 교착을 막기 위해 그 이후에는 감사 기록과 경고를 남기고 통과시킵니다"로 수정되어 코드의 실제 동작(SCORE-01 / CAP 로직)과 완벽하게 일치합니다. 단일 배율 과장 재유입도 없습니다.
- **Stale 문구 제거**: "v2.1 예정"이라는 확정되지 않은 낡은 문구가 "버전이 정해지지 않은 설계 단계(ADR 대상, STATE-01)"로 정직하게 변경되었습니다.

## 2. PKG-01: Console Scripts & Version 동작 검토

- **진입점(Entry-points)**: `pyproject.toml`에 `smtw`와 `fable-lite` 진입점이 `fable_lite.cli:main`으로 올바르게 선언되었습니다. `test_packaging_entry_points.py`에서 해당 대상이 실제로 Import 및 호출 가능(Callable)한지 동적으로 검증하고 있어 안전합니다.
- **`version` 서브커맨드**: `importlib.metadata.version`을 통해 설치된 패키지 버전을 우선 시도하고, 실패(소스 체크아웃 상태) 시 `pyproject.toml`을 직접 파싱하는 Fallback이 구현되어 양쪽 환경 모두에서 안정적으로 버전을 반환합니다.

## 3. Review Debt 종결표 (13개 Finding) 스팟체크

- **해시 및 PR 매핑**: 작성된 13개 Finding의 수정 커밋과 머지 커밋(예: CODEX-01 커밋 `75a51cd` 및 PR #10 머지 `166ccbe`, AUDIT-01의 `a4774e6` 등)이 실제 Git 히스토리와 정확히 일치합니다.
- **정직한 기록 (CODEX-01/02)**: 지시서에 기재된 실패 시나리오가 트리아지 시점 기준 이미 다른 커밋(`591eedd`)의 부수 효과로 실재하지 않았음(`NOT_REPRO`)을 정직하게 밝히고, 다만 코드의 구조적 보정 누락은 실결함이 맞으므로 수정했음을 타당하게 설명하고 있습니다. 과잉 주장이 전혀 없습니다.

**최종 결론**: 설명서가 코드의 실제 동작(Opt-in, Fail-open)을 진실되게 반영하도록 성공적으로 수정되었으며, 패키지 진입점 설정 및 리뷰 부채 종결 문서 또한 한 치의 과장 없이 정교하게 작성되었습니다. APPROVE 합니다.
