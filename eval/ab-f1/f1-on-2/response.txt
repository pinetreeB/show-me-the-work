조사 팩 마커를 정확한 형식으로 다시 기록합니다.

**재현:** `python demo.py` → `2 + 3 = -1` (기대 `5`).

**가설 1:** `add`가 뺄셈(`a - b`)을 수행한다.
**증거:** `calc.py:2` = `return a - b`. `2 - 3 = -1`로 관측값과 정확히 일치. 확정.

**가설 2:** `demo.py`의 인자 순서가 뒤바뀌었다.
**증거:** 순서가 바뀌었다면 `3 - 2 = 1`이 나와야 하나 실제는 `-1`.
**기각:** 관측값 불일치로 기각.

**가설 3:** f-string 포맷 문자열 오류.
**증거:** 출력의 "2 + 3 =" 텍스트는 정상, 계산 결과만 틀림.
**기각:** 텍스트 정상이므로 기각.

**수정:** `calc.py:2` `return a - b` → `return a + b`
**전후 검증:** 수정 전 `2 + 3 = -1` → 수정 후 `2 + 3 = 5`. 정상.

`add` 함수가 이름과 달리 뺄셈을 하고 있었던 것이 원인이었고, 덧셈으로 고쳐 해결했습니다.