조사 팩 마커를 정리해 다시 보고합니다.

**재현:** `python demo.py` 실행 시 `2 + 3 = -1` 출력 (기대값 `5`).

**가설 1:** `add`가 `a + b`가 아니라 `a - b`로 구현되어 뺄셈이 실행된다.
**가설 2:** `demo.py`가 잘못된 함수를 import 한다.
**가설 3:** f-string 포맷 문자열 오류.

**증거:**
- 가설 1 → `calc.py:2`가 `return a - b`. 실행 결과 `add(2,3) = -1` (`2-3=-1`과 일치). **확정.**
- 가설 2 → `from calc import add`로 올바른 함수 import 확인.
- 가설 3 → `{add(2, 3)}` 정상 문법 확인.

**기각:** 가설 2 (import 정상), 가설 3 (문법 정상).

**수정:** `calc.py:2` `return a - b` → `return a + b`

**전후 검증:** 수정 전 `2 + 3 = -1` → 수정 후 `2 + 3 = 5` ✅