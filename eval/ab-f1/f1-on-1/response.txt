조사 팩 마커를 갖춰 다시 정리합니다.

**재현:** `python demo.py` → 기대값 `2 + 3 = 5`, 실제 `-1` 반환 확인.

**가설 1:** 연산자 오타 — `add`인데 본문이 `return a - b`. → 채택 (증거로 확정)
**가설 2:** 인자 순서 문제. **기각:** `a - b`는 순서와 무관하게 덧셈이 아니므로 원인 아님.
**가설 3:** `demo.py` 호출부(`add(2, 3)`) 오류. **기각:** 호출·포맷은 정상, 문제는 함수 본문에 있음.

**증거:**
- `calc.py:2` 원본 `return a - b` — 함수명 `add`와 동작 불일치.
- 수정 후 `python demo.py` 실행 → `2 + 3 = 5` 정상 출력.

**기각:** 가설 2, 3은 위 증거로 배제됨.

**인과사슬:** 뺄셈 연산자 → `add(2,3) = -1` → demo 출력이 표시(`2 + 3`)와 모순.

**수정:** `calc.py:2` `return a - b` → `return a + b`. 전후 검증 완료(실행 관측).